```sql
SELECT
    id,
    COUNT(*) AS num
FROM
    (
        -- Get all requesters
        SELECT requester_id AS id FROM RequestAccepted
        UNION ALL
        -- Get all accepters
        SELECT accepter_id AS id FROM RequestAccepted
    ) AS AllFriendships
GROUP BY
    id
ORDER BY
    num DESC
LIMIT 1;
```
