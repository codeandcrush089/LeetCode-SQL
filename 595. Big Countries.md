# Problem 595: Big Countries
![Easy](https://img.shields.io/badge/Level-Easy-brightgreen)

```sql
SELECT
    name,
    population,
    area
FROM
    World
WHERE
    area >= 3000000 OR population >= 25000000;
```
---
## 🧐 Query Explanation

### **1. `FROM World`**

Reads country data from the `World` table.


### **2. Filter condition**

```sql
area >= 3000000 OR population >= 25000000
```

* Selects countries with a **large area** (≥ 3,000,000) **or**
* A **large population** (≥ 25,000,000).


### **3. `SELECT name, population, area`**

Returns the country’s name along with its population and area.

