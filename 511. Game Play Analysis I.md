```sql
-- Solution to find the first login date (minimum event_date) for each player.

SELECT
    player_id,
    MIN(event_date) AS first_login
FROM
    Activity
GROUP BY
    player_id
ORDER BY
    player_id;
```
