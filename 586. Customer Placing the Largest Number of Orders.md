```sql
SELECT
    customer_number
FROM
    Orders
GROUP BY
    customer_number  -- Group the rows by customer_number
ORDER BY
    -- Count the number of orders (rows) in each group and order by this count descending
    COUNT(order_number) DESC
LIMIT 1; -- Select only the top customer
```
