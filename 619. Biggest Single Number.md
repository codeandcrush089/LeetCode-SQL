```sql
SELECT
    MAX(num) AS num
FROM
    MyNumbers
WHERE
    num IN (
        -- Subquery to find all single numbers (numbers that appear exactly once)
        SELECT
            num
        FROM
            MyNumbers
        GROUP BY
            num
        HAVING
            COUNT(num) = 1
    );
```
